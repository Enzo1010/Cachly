spring:
  application:
    name: backend
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/cachly}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:123}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    show-sql: false

api:
  security:
    token:
      secret: segredo-apenas-para-testes-do-cachly-com-tamanho-suficiente
      expiration-hours: 24
