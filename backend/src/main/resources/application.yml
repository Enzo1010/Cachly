spring:
  application:
    name: backend
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/cachly}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    show-sql: false

api:
  security:
    token:
      secret: ${JWT_SECRET:1abd8fa8fbf1b184566e1da823f3558efa554e31fe6dd8c5ee5d9273a9eb88ee}
      expiration-hours: ${JWT_EXPIRATION_HOURS:24}

app:
  timezone: America/Sao_Paulo

ranking:
  historico:
    top-n: 10
